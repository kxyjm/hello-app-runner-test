version: 1.0
runtime: python3
build:
  commands:
    build:
    - echo "Test build 1" && yum install -y pycairo
    - echo "Test build 2" && pip install -r requirements.txt
run:
  command: echo "Test run" && python app.py
  network:
    port: 8000
